pipeline {
    agent any

    stages {
        stage('Test Docker') {
            steps {
                nodejs('node-22') {
                    sh '''
                    cd app
                    npm ci
                    npm test
                    '''
                }
            }
        }
    }
}
